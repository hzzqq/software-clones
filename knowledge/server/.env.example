# Server port (injected by launcher; default shown)
PORT=4224
# Allowed CORS origin (frontend dev server)
CORS_ORIGIN=http://localhost:5204
# Absolute or relative path to the SQLite database file
DB_PATH=./data/app.db
