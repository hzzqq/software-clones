# Server port (injected by launcher; default shown)
PORT=4223
# Allowed CORS origin (frontend dev server)
CORS_ORIGIN=http://localhost:5203
# Absolute or relative path to the SQLite database file
DB_PATH=./data/app.db
# Directory for uploaded image-node files (served at /api/uploads)
UPLOAD_DIR=./data/uploads
